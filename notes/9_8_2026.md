# 9/8/2026 — Frontend + admin backlog

Added Next.js UI, then registration/CRUD, Alembic, and provider/platform workspaces.

## Ports

| Service | Port |
|---|---|
| FastAPI | **8102** |
| Next.js | **3100** |
| Qdrant | **6433** / **6434** |

```bash
uvicorn backend.main:app --reload --port 8102
cd frontend && npm run dev
```

## Frontend routes

- `/login`, `/register`
- `/plans`, `/comparisons/[id]`, `/chat`
- `/employer` — employer requirements
- `/provider` — create/edit plans (terms + pricing) + hospitals
- `/platform` — orgs + users

## Rich plan editor (follow-up)

- `PlanEditorForm`: name, coverage_type, waiting/maternity/PEC, exclusions, sub-limits, pricing tiers
- Create + Edit via `POST /plans` and `PATCH /plans/{id}` (`updatePlan` client helper)
- Hospital form now includes tier select

## Admin APIs

- `POST /auth/register`, `GET /auth/me`
- `GET|PATCH /employers/me`
- `GET /providers/me`, `POST /providers/me/hospitals`
- `POST|PATCH /plans`
- `GET|POST /organizations`, `POST /users`

## Alembic

- `alembic.ini` + `alembic/versions/0001_initial.py`
- Existing DB: `alembic stamp head`
- Fresh: `alembic upgrade head`
- **Prod / Docker:** `AUTO_CREATE_TABLES=false` + entrypoint `alembic upgrade head` (no `create_all`)
- **Local default:** `create_all` still allowed via `AUTO_CREATE_TABLES=true`

## Deploy Phase A (same day)

- `backend/Dockerfile`, `frontend/Dockerfile`, `.env.example`, `.gitignore`
- Compose profile `app`: `api` (`8102→8000`) + `web` (`3100`)
- Run: `docker compose --profile app up -d --build` from `infra/`
- Next.js `output: "standalone"` for the web image

## PEC fix (same day)

Pricing scrub / multi-plan retrieve / verify guard so waiting-period Q&A cites all plans.

## LangGraph upgrade (same day)

Made the agent a real LangGraph supervisor (not a thin linear shell):

- **Supervisor** → `policy` | `compare` | `refuse` subgraphs
- **Policy subgraph**: rewrite → parallel retrieve (`Send`) → grade → retry → generate → verify  
  (CRAG pipeline — right for grounded booklet Q&A; not open ReAct)
- **Compare subgraph**: mandatory `run_comparison` tool first, then retrieve + narrative  
  (rules engine stays source of truth for eligibility/ranking)
- Tools: `retrieve_policy`, `run_comparison`
- Multi-turn history in Redis (`agent:thread:{org}:{thread}:messages`)

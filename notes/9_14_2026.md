## 2026-09-14

### Prioritized Roadmap

- Turned the "what's been done / what's the target" review into a prioritized next-steps plan: Phase 0 (commit existing work) → Phase 1 (CI + rate limiting) → Phase 2 (auth follow-ups: email verification, password reset, invite-based provider onboarding) → Phase 3 (generated TypeScript client) → Phase 4 (seed-data factory) → Phase 5 (billing/tenancy, deliberately last — biggest scope jump, and it assumes auth/rate-limiting/CI are already solid).
- Declined a request to run all 6 remaining phases as parallel subagents against the same repo: rate limiting and auth follow-ups both edit `backend/api/v1/auth.py`/`backend/core/config.py` (real conflict risk), and billing is large enough to deserve its own planning pass rather than a parallel lane. Agreed instead to parallelize only Phase 1 (CI + rate limiting), which have minimal file overlap.

### Phase 0 — Committed Prior Uncommitted Work

- Split the auth-hardening and RAG-hardening work (both built and verified live in the previous session but never committed) into three commits: auth hardening (`b02d9d7`), RAG hardening (`a2f4987`), and docs (`5d5c53d`). Pushed all three to `origin/main`.

### Phase 1 — CI + Rate Limiting

- Attempted git-worktree isolation for the two parallel agents; unavailable in this environment ("not in a git repository and no WorktreeCreate hooks are configured" despite this being a real git repo — looked into configuring the hooks, decided against guessing at an undocumented `WorktreeCreate`/`WorktreeRemove` contract for a nice-to-have, not a blocker).
- Ran both agents in parallel directly against the working directory instead, with manually scoped file boundaries (CI agent: `.github/`, root lint config, append-only `requirements.txt`; rate-limiting agent: `backend/main.py`, `backend/core/config.py`, `backend/api/**`) to avoid conflicts.
- **CI agent**: added `.github/workflows/ci.yml` (backend: ruff + pytest; frontend: eslint + `tsc --noEmit`) and a root `pyproject.toml` ruff config. Confirmed `pytest -q` needs no live services. Found 18 pre-existing lint issues (unsorted imports, unused imports, one redefinition, `timezone.utc` → `datetime.UTC` modernization) — left unfixed by the agent (out of its scope), fixed afterward via `ruff --fix` so CI starts green; full suite reconfirmed passing (67/67) after the fix.
- **Rate-limiting agent**: added `backend/core/rate_limit.py` (Redis-backed `slowapi` `Limiter`, reusing `Settings.redis_url`) protecting `/auth/login` (5/min), `/auth/register` (3/min), `/auth/refresh`/`/auth/logout` (20/min, per-IP), and `/rag/query`/`/agents/chat` (10/min, per authenticated user). Verified live against real Redis: exceeding the login limit returns a real `429` with `Retry-After`, and the limiter's key was confirmed written to the shared Redis instance.
- Both agents' changes merged with zero real conflicts — only the expected shared `requirements.txt` append (both lines landed cleanly, one after the other).
- Committed as three commits: CI pipeline (`a163fb8`), rate limiting (`11dd5dd`), `CLAUDE.md` known-gaps update (`975782f`). Not yet pushed.

### Phase 2 — Auth Follow-ups

- **Email verification**: `POST /auth/register` now creates users with `email_verified=false` and fires a verification email; `POST /auth/email/verify` and `POST /auth/email/resend-verification` (always 202, no user enumeration) complete the loop. Non-blocking by design — `/auth/me` exposes `email_verified` for the frontend to nag on, but an unverified user can still log in. Seeded demo users are pre-verified.
- **Password reset**: `POST /auth/password-reset/request` (always 202) + `POST /auth/password-reset/confirm` (30 min, single-use token). Confirming a reset revokes every existing refresh token for that user — treated as compromise recovery, forces re-login everywhere.
- **Invite-based provider onboarding**: healthcare providers can no longer self-register (`POST /auth/register` now rejects `org_type=healthcare_provider`). A platform admin creates an invite (`POST /invites`), the invitee previews it publicly (`GET /auth/invites/{token}`) and accepts (`POST /auth/invites/{token}/accept`), which creates the org + `HealthcareProvider` profile + pre-verified `healthcare_org_admin` user and logs them in. `GET /invites` / `POST /invites/{id}/revoke` manage them from the platform-admin side.
- New `backend/core/email.py`: console-logs in dev by default (no SMTP provider in `infra/docker-compose.yml`), or real SMTP via stdlib `smtplib` (`EMAIL_BACKEND=smtp`) — deliberately no vendor SDK, same "don't pick a SaaS vendor yet" posture as the Stripe/billing gap.
- New `backend/services/auth_flows.py` holds token issuance/consumption for all three flows, kept separate from tenant CRUD in `backend/services/admin.py`.
- New tables via `alembic/versions/0007_auth_followups.py`: `email_verification_tokens`, `password_reset_tokens`, `provider_invites`, plus `users.email_verified`. Applied manually to the local dev DB (`ALTER TABLE ... ADD COLUMN`) since this DB tracks `create_all`, not `alembic upgrade head` (confirmed `alembic current` was still pinned at `0001_initial` even though tables through migration `0006` already existed — local dev has never actually run `alembic upgrade head`, only `create_all`).
- Rate limits added for all new public endpoints (`rate_limit_verify_email`, `rate_limit_resend_verification`, `rate_limit_password_reset_request`, `rate_limit_password_reset_confirm`, `rate_limit_invite_accept`).
- 21 new tests in `tests/test_auth_flows.py` (pure logic + auth-shape checks, no live DB needed, matching the existing suite's style); full suite at 84/84, ruff clean.
- **Bug caught during live smoke-testing** (register → verify → reset → invite-accept, all exercised against real Postgres): four new endpoints 500'd because slowapi's `headers_enabled=True` needs a `response: Response` parameter declared to inject rate-limit headers into, or it raises — the exact gotcha already documented in `rate_limit.py`'s docstring for the *existing* routes, which I missed on the new ones first pass. Fixed and reverified live.
- Also fixed a latent, unrelated-to-the-task visibility bug in the new console email backend: it originally logged at `INFO`, which is silently dropped since this app has no root logging config anywhere (confirmed other pre-existing `logger.info` calls, e.g. the indexing-queue "Enqueued N documents" log, never appear either) — switched to `logger.warning` so the token is actually visible in dev.
- Cleaned up all test users/orgs/invites created during live smoke-testing from the dev DB afterward.
- Committed as two commits: backend implementation (`0b605f7`), `CLAUDE.md`/notes update (`c367915`). Pushed to `origin/main`.

### Phase 2 — Frontend Pages

- New pages: `/verify-email` (reads `?token=`, calls verify on mount, success/error state), `/forgot-password` (request form, always shows the generic "if that email exists…" message), `/reset-password` (`?token=` + new password, redirects to `/login` after success), `/accept-invite` (previews the invite by token, shows org/email, password form, then the same fetchMe→setMe→homeForRole redirect the register page uses).
- `register` page: dropped the org-type selector — self-registration is employer-only now that the backend rejects `healthcare_provider`; copy points providers at the invite flow instead.
- `AppShell`: added an unverified-email nudge banner (amber, with a resend button hitting `/auth/email/resend-verification`) shown whenever `me.email_verified` is false; resets per-navigation.
- Platform admin page: added a provider-invites panel (table with status chip + revoke button, plus a create-invite form) above the existing organization/user management panels.
- `frontend/src/lib/{api,types,auth}.ts` extended with the 9 new endpoint calls, `ProviderInvite`/`ProviderInvitePreview` types, and `email_verified` on `MeUser`/`StoredMe`.
- Added `.success-banner` / `.warning-banner` to `globals.css` (existing `.error-banner` had no positive/caution counterpart).
- Verification: `tsc --noEmit` and `eslint` both clean; started the Next dev server and curled all four new routes (200s, correct SSR content, no console/server errors in the Next log). No live browser available this session (user declined the Chrome extension install) — instead ran the exact fetch sequence the `accept-invite` page's client code performs (`GET .../invites/{token}` → `POST .../accept` → `GET /auth/me`) via curl with the real CORS `Origin: http://localhost:3100` header against the live backend, confirmed cookies + response shapes match what the page expects end-to-end, then cleaned up the test invite/org/user.
- `CLAUDE.md`'s Auth section and known-gaps entry updated to say this phase is backend **and** frontend complete, not backend-only.

- Committed as `5e8c7dc`. Pushed to `origin/main`.

### Phase 3 — Generated TypeScript Client

- Checked whether the schema-fetch step needed a live server: `app.openapi()` only
  introspects routes/Pydantic models and every `Settings` field already has a default, so
  importing `backend.main.app` and dumping the schema needs no DB/Redis/Qdrant — no live
  server, no `docker compose up`, in either local dev or CI. Went with that (a static
  `openapi.json` export) over spinning up a live server.
- Audited every endpoint the frontend calls for a missing `response_model` first, per the
  task: all JSON-returning ones already had one. The only gaps were the two SSE streams
  (`/agents/chat`, `/rag/query`, no `response_model` by design) and the raw-bytes
  `/documents/{id}/content` download — none of which the frontend treats as typed JSON
  anyway, so no backend changes needed there.
- Added `scripts/export_openapi.py` (imports `backend.main.app`, writes `openapi.json` to
  repo root, gitignored) and `frontend`'s `generate:types` npm script
  (`python ../scripts/export_openapi.py && openapi-typescript ../openapi.json -o
  src/lib/generated-types.ts`); `openapi-typescript@^7.13.0` added as a frontend
  devDependency. `generated-types.ts` itself **is** committed (it's the artifact the
  frontend build actually needs; only the intermediate `openapi.json` is gitignored).
- Rewrote `frontend/src/lib/types.ts` to re-export/wrap `generated-types.ts` instead of
  hand-declaring backend-mirrored shapes. Kept hand-written: `AgentSseEvent`/
  `SourceChunk`/`AgentChatFinal` (no `response_model` to derive from — the SSE routes
  aren't in the schema at all) and two deliberate narrowings over what the backend
  under-types — `ProviderInvite.status` (plain `str` in the backend schema; DB check
  constraint, not a Pydantic enum) and `PlanVersion.extraction_metadata` (untyped `dict`,
  like `raw_terms`).
- Updated `frontend/src/lib/api.ts` to use the generated `*CreateRequest`/
  `*UpdateRequest`/`*Read` schema types for request bodies and responses instead of the
  old hand-typed inline shapes (`PlanCreateRequest`, `PlanUpdateRequest`,
  `HospitalCreateRequest`, `EmployerUpdateRequest`, `OrganizationCreateRequest`,
  `UserCreateRequest`, `RegisterRequest`, `LoginRequest`, `TokenResponse`, and the various
  auth-flow request schemas) — verified each request schema's actual field names against
  the exported `openapi.json` before swapping (e.g. `PasswordResetConfirmRequest.new_password`,
  `ComparisonCreateRequest.plan_ids`) rather than assuming they matched.
- **Bug caught by the stricter generated types, not previously known:** `PlanVersionRead`
  (`backend/schemas/policy.py`) never declared `extraction_metadata`, even though the
  `PlanVersion` ORM model has always had that column (`backend/models/entities.py`) and
  `frontend/src/app/(app)/provider/page.tsx`'s "Review terms" panel has always read
  `version.extraction_metadata.evidence` off the API response. Runtime effect: opening
  that panel on any real plan version would throw (`Cannot read properties of undefined`)
  — the hand-written frontend type had just been silently asserting the field existed.
  Fixed by adding `extraction_metadata: dict` to `PlanVersionRead` (all three endpoints
  that return it use `model_validate(version)` with `from_attributes=True`, so no service
  code changes were needed) — the same category of fix as auditing `response_model`
  completeness, just one level deeper (a field missing from an existing `response_model`
  rather than a whole missing `response_model`).
- Also fixed two more `tsc` failures the stricter generated types surfaced (both pre-existing,
  unrelated to the endpoint audit): `platform/page.tsx`'s `userRole` state was untyped
  `string` instead of `UserRole` (same pattern already used for `orgType` one line above —
  just never applied to the role field), and `comparisons/[id]/page.tsx` treated
  `ComparisonResultRead.citations` as always-present when the backend schema (correctly)
  has it optional with a default.
- Added a CI step (frontend job, `.github/workflows/ci.yml`) between lint and typecheck:
  installs backend deps, reruns `generate:types`, and fails the build on
  `git diff --exit-code -- src/lib/generated-types.ts` if the committed file is stale.
- Verification: `tsc --noEmit` and `eslint` both clean after the fixes above; full backend
  suite 84/84, ruff clean. Attempted a live round-trip smoke test (create a plan version
  with `extraction_metadata`, then read it back) but hit unrelated pre-existing local dev
  DB drift — `plan_versions` in the actual Postgres instance is missing a `coverage_type`
  column entirely, so both the insert and a plain `SELECT` on that table 500 regardless of
  this change (same "local dev has never run `alembic upgrade head`" issue noted in the
  Phase 2 entry above, now confirmed to affect `plan_versions` specifically, not just the
  new auth tables). Didn't touch the dev DB to work around it — out of scope for this
  task. Confirmed correctness instead via the full test suite, the OpenAPI schema
  reflecting the new field correctly, and the identical `from_attributes` mechanism
  already working for `raw_terms`/`normalized_terms`.
- `CLAUDE.md`: removed the "no generated TypeScript client" known-gap line, added a new
  bullet under Architectural boundaries documenting the generate/CI-check workflow.

### Git History Rewrite — Stripped Claude Attribution Trailers

- User added a "Git commit conventions" rule to `CLAUDE.md` banning
  `Co-Authored-By: Claude` and `Claude-Session` trailers, then asked to retroactively
  strip both from every commit in history (solo project, force-push accepted).
- Surveyed the exact wording first: all 12 affected commits used the identical two-line
  pair `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>` /
  `Claude-Session: https://claude.ai/code/session_...` — no variants. One unrelated line
  ("Claude Code /doctor cleanup pass.") is part of a real commit description, not a
  trailer — confirmed it should NOT be touched.
- Backed up first: `git bundle create backup.bundle --all` (still sitting at repo root,
  untracked, as the rollback path).
- `git-filter-repo` wasn't installed; installed it via
  `.venv/Scripts/python.exe -m pip install git-filter-repo` (v2.47.0). Ran it with a
  `--message-callback` regex stripping any line starting with `Co-Authored-By: *Claude`
  or `Claude-Session: *` from every commit message, `--force` (required since this isn't
  a fresh clone).
- Verified zero matches remain (checked the trailer text, the email, and the session-URL
  pattern independently) and that all 13 commits survived (12 original + the CLAUDE.md
  convention-update commit made just before the rewrite, to get a clean working tree for
  `filter-repo`).
- `filter-repo` removes the `origin` remote as a safety default — re-added it
  (`git remote add origin https://github.com/AnshaSarwar/healthcare_comparison_platform.git`)
  before pushing.
- Force-pushed: `git push --force --all` then `--tags` (no tags existed, so that one was
  a no-op). New `main` head: `8b66a8d`.

### `/ship` Project Skill

- Added `.claude/skills/ship/SKILL.md` at the user's request — a checklist (run tests +
  lint, smoke-test new endpoints, update notes/CLAUDE.md, check for nested `.git` under
  `frontend/`, commit + push) that mirrors what's already in CLAUDE.md's "Definition of
  Done" and "Repo Hygiene" sections. Not committed yet.
- Confirmed it's **not usable this session** — tried invoking it directly and got
  `Unknown skill: ship`. Project skills are enumerated once at session start; a file
  created mid-session needs a new session (or restart) to show up. Same root cause as the
  hooks-watcher caveat below.

### Ruff Auto-Format Hook

- User supplied an exact `.claude/settings.json` snippet: a `PostToolUse` hook on
  `matcher: "Edit|Write"` running `ruff check --fix . && ruff format .` after every
  edit/write. No `.claude/settings.json` existed yet (only `settings.local.json`), so
  this was a clean create, no merge needed.
- Pipe-tested the raw command against the real repo before writing it — and it mutated
  13 tracked files (`alembic/`, `tests/`) with real (if cosmetic — import ordering,
  blank-line collapsing) changes, because `ruff check --fix` has no dry-run mode. That's
  outside CI's current lint scope (`ruff check backend` only), so those files had never
  been linted before. Reverted the accidental changes immediately (`git checkout --` on
  the affected files) since this was a side effect of testing, not a requested edit —
  working tree confirmed clean again afterward.
- Wrote `.claude/settings.json` with the hook exactly as given. Validated with `python -c
  "json.load(...)"` (no `jq` available in this shell) — matcher/command parse correctly.
- Tried to prove the hook actually fires: wrote a scratch file
  (`scripts/_hook_proof.py`) with an unsorted-imports + double-blank-line violation via
  the Write tool, then re-read it. Unchanged — hook did not fire. Same cause as the
  `/ship` skill: `.claude/settings.json` didn't exist when this session started, so the
  settings watcher isn't watching it. Cleaned up the scratch file.
- Flagged to the user (not yet resolved): the hook runs `ruff` across the **whole repo**
  on every single edit, not just `backend/` — matches what they asked for literally, but
  is a mismatch with CI's scope and will be slower than necessary if left as-is. Offered
  to narrow it to `backend/` if they want.
- Neither `.claude/settings.json` nor `.claude/skills/ship/SKILL.md` are committed yet.

### Local Dev DB Drift — Fixed

- Fixed the `plan_versions.coverage_type` drift found during Phase 3 verification.
  Root cause confirmed: local dev has only ever used `create_all` (`AUTO_CREATE_TABLES=
  true`), which creates missing tables but never adds columns to ones that already
  exist — so `plan_versions` was stuck at whatever shape it had the first time
  `create_all` ran, missing every column added to the model since.
- Chose to drop and recreate rather than patch columns table-by-table: checked row
  counts first (5 users, 4 orgs, a handful of plans/comparisons, `plan_versions` already
  empty since inserts had been failing) — confirmed disposable demo/smoke-test data, not
  anything worth preserving column-by-column.
  This action needed explicit user confirmation — the auto-mode classifier blocked the
  first attempt as "Irreversible Local Destruction" even after the user had said "please
  fix it" generically; re-asked with the specific blast radius (which DB, which tables,
  what data) and got an explicit go-ahead before proceeding.
- Dropped all 14 app tables via `Base.metadata.drop_all` (the same metadata `init_db()`
  already uses), then started the backend so its own `init_db()` (`create_all`) +
  `seed_data`/`seed_plan_documents` rebuilt the schema from current models and reseeded
  the demo tenant.
- Verified with the exact live smoke test that failed during Phase 3: `POST
  /plans/{id}/versions` with an `extraction_metadata` payload → `201`, then `GET` the
  same version back → `extraction_metadata` present in the response. Confirms the
  Phase 3 `PlanVersionRead` fix works end-to-end now that the DB matches the model, not
  just in the OpenAPI schema.
- Along the way, found a second, separate, pre-existing quirk: `create_plan_version` in
  `backend/services/admin.py:258` hardcodes `extraction_metadata={}` and ignores whatever
  the caller submits in that field (unlike the document-extraction path in
  `backend/services/document.py`, which populates it from real LLM extraction). Flagged
  but not fixed — plausibly intentional (a manually-created version has no extraction
  evidence to speak of), and the frontend never calls this endpoint with that field
  anyway, so no user-facing impact. Worth a decision later, not a bug fix right now.
- Cleaned up the smoke-test plan version row, stopped the backend. Full suite reconfirmed
  84/84, ruff clean, after the DB rebuild.
- Killing the backend process hit a Windows quirk worth remembering: `Stop-Process`
  killed the uvicorn PID but `Get-NetTCPConnection` kept reporting the (now-nonexistent)
  PID as still owning port 8102, and the server kept answering requests. Root cause was
  an orphaned `multiprocessing`-fork child process (reparented after its uvicorn parent
  died) still holding the listening socket; killing that child process by its own PID
  actually freed the port.
- Resolved the `create_plan_version` / `extraction_metadata={}` quirk from above: decided
  it was an oversight, not intentional design, because `review_plan_version` requires
  evidence found inside `extraction_metadata` before it will approve a version — with the
  field hardcoded to `{}`, every manually-created plan version was permanently stuck
  below `APPROVED` (rejectable or correctable, never approvable). Fixed
  `backend/services/admin.py::create_plan_version` to store `body.extraction_metadata`
  instead of `{}`; `PlanVersionCreateRequest` already exposed the field, so no schema
  change needed. Verified full 84/84 suite + ruff clean on the touched files, then an
  end-to-end smoke test (register/login as `admin@healthfirst.com`, `POST
  /plans/{id}/versions` with a fabricated evidence payload covering all six
  required fields at confidence 0.9, `GET` back to confirm the submitted
  `extraction_metadata` round-tripped verbatim, then `PATCH
  /plan-versions/{id}/review {review_status: approved}` succeeded) — confirms manually
  entered versions can now actually reach `APPROVED`. Cleaned up the smoke-test row
  afterward (confirmed `plan_versions` was empty beforehand, so no seed data was
  disturbed by the review endpoint's side effect of archiving other approved versions on
  the same plan).

### Phase 4 — Seed-Data Factory

- Ran a strict test-first loop end to end: read `backend/models/entities.py` and the
  original `backend/db/seed.py` first (no Faker/factory library in `requirements.txt`,
  and the old idempotency check was crude — bail out entirely if *any* user row
  exists). Wrote `tests/test_seed.py` against a target API (`SeedConfig`, `seed_data`,
  `main`) that didn't exist yet, confirmed it failed on `ImportError`, then implemented.
- No existing test in the repo touched a real database (everything was pure
  unit/in-memory), so this needed new DB test infrastructure: `tests/conftest.py`
  provisions a dedicated scratch database (`benefits_compare_seedtest`) on the same
  local Postgres instance via `asyncpg`, drops/recreates tables per test function for
  isolation. Deliberately a separate DB from `benefits_compare` (the real dev DB) —
  `Organization.name`/`User.email` are unique, so accidentally colliding with real dev
  data would have made idempotency untestable.
- Rewrote `backend/db/seed.py`: kept the original hand-authored Acme/HealthFirst/
  MediCare dataset verbatim (same login emails documented in CLAUDE.md — other scripts/
  manual QA depend on them), refactored its idempotency to be per-entity (checks each
  org by name) rather than the old all-or-nothing "any user exists" bail-out, then
  layered a `SeedConfig`-driven factory on top: `SeedConfig.demo()` adds a small amount
  of generated variety (extra employers/providers, mixed coverage types, one forced
  zero-plan provider as the edge case); `SeedConfig.load_test(tenant_count=N)` skips the
  fixed accounts and generates large synthetic tenant counts. No Faker dependency added
  — used small curated word lists + a seeded `random.Random`, since names only need to
  look plausible, not be an exhaustive corpus, and adding a new dependency for this
  would have been overkill.
- Idempotency design: entity "spec" generation (name, demographics, plan terms) is a
  pure function of `(seed, index)` and always runs in full, *then* a check-then-insert
  looks up the org by name and skips if it already exists — so re-running with the same
  config produces byte-identical names every time and never desyncs the RNG sequence
  between runs (the alternative — skipping RNG draws when an entity already exists —
  would have made every entity generated *after* the first skip diverge from prior
  runs).
- 10 new tests in `tests/test_seed.py`: fixed accounts still created, extra variety
  beyond the 3 fixed orgs, zero-plan-provider edge case present, idempotent re-run
  yields identical row counts, referential integrity (org type ↔ employer/provider ↔
  user role ↔ plan/hospital `provider_id`), load-test mode hits exact requested tenant/
  provider counts and excludes the fixed accounts, CLI arg parsing, and a full CLI
  `main()` run against the scratch DB. One test bug caught during the loop (not an
  implementation bug): compared `load_summary.employers - demo_summary.employers`
  against 25, forgetting each `seed_data()` call returns a **fresh** `SeedSummary` for
  that call alone, not a running total — fixed the assertion, not the implementation.
- Full loop: `pytest -x -q` → 94/94 passed; `ruff check` clean on all touched files
  (repo-wide `ruff check .` still shows 42 pre-existing errors, all in `alembic/`,
  `scripts/`, and unrelated test files predating this change — left untouched, out of
  scope); manual CLI smoke test against a separate throwaway `benefits_compare_
  manual_smoke` DB confirmed demo mode (idempotent — second run reported
  `skipped_existing=7`, added zero new rows), load-test mode at 200 employers / 40
  providers in ~3.5s, coverage-type variety across all three enum values, 9 zero-plan
  providers (1 forced + naturally-random ones, since `load_test` allows
  `min_plans_per_provider=0`), and zero FK-orphaned rows.
- Checked the previously-flagged known gap directly rather than assuming it still
  applied: the local dev Postgres `plan_versions` table already has the `coverage_type`
  column (confirmed via `\d plan_versions` — presumably fixed by the DB rebuild in the
  "Local Dev DB Drift" entry above, in an earlier session). Moot for this task either
  way, since the seed factory never creates `PlanVersion` rows.
- `CLAUDE.md`: replaced the "seed data is a single hardcoded tenant" known-gap bullet
  with a description of the finished factory (CLI flags, idempotency mechanism, scratch-
  DB test setup).
- Committed as `86b5c03`. **Not yet pushed** — `git push` was declined by the session's
  permission settings; needs a manual push or an explicit re-approval next session.

### Next Steps

- Push `86b5c03` (seed-data factory) to `origin/main` — declined by permission settings
  this session, not yet retried.
- Decide whether to narrow the ruff auto-format hook to `backend/` only (still runs
  repo-wide on every edit as of the last check).
- The 42 pre-existing `ruff check .` errors in `alembic/`, `scripts/`, and a few test
  files (outside CI's current `ruff check backend` scope) are still unaddressed —
  cosmetic (import ordering, etc.), not urgent, but worth a cleanup pass eventually so
  `ruff check .` is green repo-wide, not just within CI's narrower scope.
- Phase 5: billing/tenancy (subscription tiers, Stripe, per-org usage limits) —
  deliberately last per the original roadmap, biggest scope jump.
